//Survey Field contains logic to render a single label
//and text input

import React from 'react';

export default ({ input }) => { //( {input} ) automatically looks onto the props object, pulls off the input property and assigns it to a variable called input
	console.log(props.input);
	return (
		<div>
			<input />
		</div>
	);
};